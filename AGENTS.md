# AGENTS.md

Always implement the requested change fully in the local codebase first.

After making changes:
1. Run the local build/dev workflow required for this repository.
2. Rebuild/restart Docker if needed so localhost reflects the update immediately.
3. Verify the app is using the new code locally.
4. Summarize exactly what changed.

Rules:
- Never stop after only editing files.
- Never leave the user needing to manually rebuild unless unavoidable.
- Do not commit or push unless I explicitly ask.
- When I say "commit and push", create a clean commit and push to the current branch.

After creating AGENTS.md, confirm it was added successfully and follow it for all future changes in this repo.
